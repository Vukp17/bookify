# For extensibility; currently we use the official postgres image directly via docker-compose
# This Dockerfile is intentionally minimal; kept for future customization (extensions, init scripts)
FROM postgres:15
